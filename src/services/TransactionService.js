const TransactionModel = require('../models/TransactionModel')
const ServiceError = require('../utils/Exception')
const {DataTypes} = require("sequelize");

module.exports = {
    async createTransaction(accountId, description, type, categoryId) {

    }
}